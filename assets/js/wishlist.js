
document.addEventListener('DOMContentLoaded',()=>{const grid=document.getElementById('wishlistGrid');function render(){const ids=Store.getWishlist(),list=ids.map(Store.byId).filter(Boolean);document.getElementById('wishHeading').textContent=`Saved pieces (${list.length})`;grid.innerHTML=list.length?list.map(p=>`<div class="col-6 col-lg-3">${Store.renderProductCard(p)}</div>`).join(''):'<div class="col-12"><div class="empty-state"><i class="bi bi-heart fs-1"></i><h2 class="h3 mt-3">Nothing saved yet</h2><p class="text-muted-sw">Use the heart on any product to keep it here.</p><a class="btn btn-primary" href="shop.html">Browse the shop</a></div></div>';Store.updateCounts();}document.addEventListener('wishlist:change',render);render();});
